mutation PublishMessageActivity(
    $activityId: Int,
    $recipientId: Int!,
    $textBody: String!
) {
    SaveMessageActivity(
        id: $activityId,
        recipientId: $recipientId
        message: $textBody
    ) {
        ...ALMessageActivity
    }
}